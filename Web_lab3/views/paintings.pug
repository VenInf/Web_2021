extends layout

block main
    h1 Painting shelf

    table
        each painting in paintings
            tr
                td
                    each val, key in painting
                        if (key === 'title')
                            - var paintingTitle = val
                        if (key === 'active' && val == "true")
                            a(href=`paintings/?id=${painting.id}`)= paintingTitle

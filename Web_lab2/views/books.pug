extends layout

block main
    table.w3-container.w3-table.w3-hoverable
        each book in books
            tr
                td
                    each val, key in book
                        if (key === 'title')
                            a(href=`books/?id=${book.id}`)= val
